import { AgeGroupArrayPipe } from './age-group-array.pipe';

describe('AgeGroupArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new AgeGroupArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
