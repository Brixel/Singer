import { RegistrationTypePipe } from './registration-type.pipe';

describe('RegistrationTypePipe', () => {
  it('create an instance', () => {
    const pipe = new RegistrationTypePipe();
    expect(pipe).toBeTruthy();
  });
});
