import { SingerEventCostFilterParametersPipe } from './singerevent-cost-filterparameters.pipe';

describe('SingereventCostFilterparametersPipe', () => {
   it('create an instance', () => {
      const pipe = new SingerEventCostFilterParametersPipe();
      expect(pipe).toBeTruthy();
   });
});
