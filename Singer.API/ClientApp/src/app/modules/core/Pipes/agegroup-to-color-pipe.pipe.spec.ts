import { AgegroupToColorPipePipe } from './agegroup-to-color-pipe.pipe';

describe('AgegroupToColorPipePipe', () => {
   it('create an instance', () => {
      const pipe = new AgegroupToColorPipePipe();
      expect(pipe).toBeTruthy();
   });
});
