import { SingerEventLocation } from './singer-event-location';

describe('SingerEventLocation', () => {
  it('should create an instance', () => {
    expect(new SingerEventLocation()).toBeTruthy();
  });
});
